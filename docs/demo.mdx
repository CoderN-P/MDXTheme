# Demo Page

This is a demo page for MDXTheme, showcasing how to use various components and styles provided by the theme.

## Example Code Block

```react:/template/components/Info.tsx:
import { Info as InfoIcon } from "lucide-react";

export default function Info({ children }: { children: React.ReactNode }) {
  return (
    <div className="info-component rounded-lg border border-gray-200 border-t-2 border-t-blue-500 bg-white px-4 py-3 ">
      <div className="flex items-start space-x-3">
        <div className="mt-1 flex-shrink-0">
          <InfoIcon className="h-5 w-5 text-blue-500 " />
        </div>
        <div className="flex-1 text-gray-600 font-medium text-sm">
          {children}
        </div>
      </div>
    </div>
  );
}
```

## Example Usage of Info Component

<Info>
  This is an example of an Info component that can be used to display important
  messages in your documentation or static site.
</Info>

## Example Warning Component

<Warning>
  This is an example of a Warning component that can be used to highlight
  warnings or important notes.
</Warning>

## Example Formula Component

<Formula formula="E=mc^2" block={true} />

## Example Link Component

[Link to MDXTheme Documentation](https://docs.neelparpia.me/mdxtheme)
